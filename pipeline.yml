pool:
  name: linux

parameters:
- name : "siteCodes"
  default :
  - "zz01"
  - "zz04"
  - "je03"

stages:
- stage: first
  jobs:
  - job: firstjob
    steps:
    - checkout: none
    - task: Bash@3
      inputs:
        targetType: "inline"
        script : |
          echo "stage first - job first - task 1"
