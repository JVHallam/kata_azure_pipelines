pool:
  name : linux

stages:
- stage: first
  jobs:
  - job: echo
    steps:
    - checkout : none
    - task : Bash@3
      inputs : 
        targetType : "inline"
        script : |
            echo "baka mitai"
