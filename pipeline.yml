pool:
  name: linux

variables:
- name: holder
  value: "Default Value"

stages:
- stage: setup
  jobs:
  - job: jobone
    steps:
    - checkout: none
    - task: Bash@3
      inputs: 
        targetType: "inline"
        script: |
          echo "Hello World!"
