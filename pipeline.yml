pool:
  name: linux

   
variables:
- name: holder
  value : "Initial Value"

stages:
- stage:
  jobs:
  - job: echo
    steps:
    - checkout: none
    - task: Bash@3
      inputs:
        targetType: "inline"
        script: |
          echo "Compile time : ${{ variables.holder }}"

    - task: Bash@3
      inputs:
        targetType: "inline"
        script: |
          echo "##vso[task.setvariable variable=holder]Updated Value"

    - task: Bash@3
      inputs:
        targetType: "inline"
        script: |
          echo "Compile time : ${{ variables.holder }}"
          echo "Run time : $( variables.holder )"
