pool:
  name : linux

stages:
- stage: first
  dependsOn : []
  jobs:
  - job : joba
    steps :
    - task : Bash@3
      inputs :
        targetType : "inline"
        script : |
          echo "Hello World"
    - task : Bash@3
      inputs :
        targetType : "inline"
        script : |
          echo "Hello World"

  - job : jobb
    dependsOn : joba
    steps :
    - task : Bash@3
      inputs :
        targetType : "inline"
        script : |
          echo "Hello World"
    - task : Bash@3
      inputs :
        targetType : "inline"
        script : |
          echo "Hello World"
      
- stage: second
  dependsOn : []
  jobs:
  - job : jobas
    steps :
    - task : Bash@3
      inputs :
        targetType : "inline"
        script : |
          echo "Hello World"
    - task : Bash@3
      inputs :
        targetType : "inline"
        script : |
          echo "Hello World"

  - job : 
    dependsOn : jobas
    steps :
    - task : Bash@3
      inputs :
        targetType : "inline"
        script : |
          echo "Hello World"
    - task : Bash@3
      inputs :
        targetType : "inline"
        script : |
          echo "Hello World"

- stage : final
  dependsOn : 
  - first
  - second
  jobs:
  - job : 
    steps :
    - task : Bash@3
      inputs:
        targetType : "inline"
        script : |
          echo "Finished!"
