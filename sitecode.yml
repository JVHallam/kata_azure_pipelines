parameters:
- name: siteCode
  type: string

stages:
- stage: ${{ siteCode }}
  condition: not(eq('${{siteCode}}', 'zz04'))
  jobs:
  - job: firstjob
    steps:
    - checkout: none
    - task: Bash@3
      inputs:
        targetType: "inline"
        script : |
          echo "${{ siteCode }}"
